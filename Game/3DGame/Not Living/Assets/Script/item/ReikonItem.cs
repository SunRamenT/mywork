using UnityEngine;

public class ReikonItem : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
  
    [Tooltip("霊魂の回復量")]
    public float recoveryAmount = 20f;
}

